import { lerp, Vector, C } from "../utils";

describe("Test ex02 - linear interpolation", () => {
  test("case1", () => {
    // expect(lerp(Vector.from(C.map([-42, 42])), Vector.from(R.map([42, -42])), 0.5)).toEqual(Vector.from(R.map([0, 0])));
    // expect(lerp(Vector.from(C.map([2, 1])), Vector.from(R.map([4, 2])), 0.3)).toEqual(Vector.from(R.map([2.6, 1.3])));
  });
});
